<!DOCTYPE html>
<html class="html antialiased">
  <%= render "shared/head" %>

  <body class="font-sans font-normal leading-normal text-gray-800 bg-gray-50 dark:bg-gray-600">
    <%= render "shared/flash_notice" %>
    <%= render "shared/header" %>

    <main class="">
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
  </body>
</html>
