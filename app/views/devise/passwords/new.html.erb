<% content_for :devise_form do %>

  <h2 class="pt-4 mb-8 text-4xl font-bold heading dark:text-gray-100 transition duration-500">Forgot your password?</h2>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="input-group">
      <%= f.label :email, class: "label dark:text-gray-100 transition duration-500" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input dark:bg-gray-200 transition duration-500" %>
    </div>

    <div class="input-group">
      <%= f.submit "Send me reset password instructions", class: "btn btn-default" %>
    </div>
  <% end %>

  <hr class="mt-6 border" />

  <%= render "devise/shared/links" %>
<% end %>

<%= render "devise/shared/form_wrap" %>
